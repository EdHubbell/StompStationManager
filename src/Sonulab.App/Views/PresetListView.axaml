<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Sonulab.App.ViewModels"
             xmlns:ui="using:FluentAvalonia.UI.Controls"
             xmlns:conv="using:Sonulab.App.Converters"
             x:Class="Sonulab.App.Views.PresetListView"
             x:DataType="vm:PresetListViewModel">
  <DockPanel>

    <!-- Icon command bar -->
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="4" Margin="8,6,8,4">
      <Button Command="{Binding RefreshCommand}" ToolTip.Tip="Refresh">
        <ui:SymbolIcon Symbol="Refresh" FontSize="16"/>
      </Button>
      <Button Command="{Binding MoveUpCommand}" ToolTip.Tip="Move up">
        <ui:SymbolIcon Symbol="ChevronUp" FontSize="16"/>
      </Button>
      <Button Command="{Binding MoveDownCommand}" ToolTip.Tip="Move down">
        <ui:SymbolIcon Symbol="ChevronDown" FontSize="16"/>
      </Button>
      <Button Command="{Binding DuplicateCommand}" ToolTip.Tip="Duplicate">
        <ui:SymbolIcon Symbol="Copy" FontSize="16"/>
      </Button>
      <Button Command="{Binding DeleteCommand}" ToolTip.Tip="Delete">
        <ui:SymbolIcon Symbol="Delete" FontSize="16"/>
      </Button>
    </StackPanel>

    <!-- Rename bar -->
    <Grid DockPanel.Dock="Bottom" ColumnDefinitions="*,Auto" Margin="8,4">
      <TextBox x:Name="RenameBox" Grid.Column="0" PlaceholderText="New name…" Margin="0,0,4,0"/>
      <Button Grid.Column="1" Content="Rename"
              Command="{Binding RenameCommand}"
              CommandParameter="{Binding #RenameBox.Text}"/>
    </Grid>

    <!-- Busy indicator -->
    <StackPanel DockPanel.Dock="Bottom" Margin="8,2" IsVisible="{Binding IsBusy}" Spacing="4">
      <ProgressBar IsIndeterminate="True" Height="4"/>
      <TextBlock Text="{Binding BusyMessage}" FontSize="11" Opacity="0.7"/>
    </StackPanel>

    <!-- Preset list -->
    <ListBox ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}" Margin="8,0">
      <ListBox.ItemTemplate>
        <DataTemplate x:DataType="vm:PresetItemViewModel">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="{Binding DisplaySlot, StringFormat='{}{0:00}'}"
                       Width="28" VerticalAlignment="Center"
                       FontFamily="Consolas,Cascadia Mono,monospace"
                       Opacity="0.6"/>
            <TextBlock VerticalAlignment="Center"
                       Opacity="{Binding IsEmpty, Converter={x:Static conv:BoolToOpacity.Instance}}">
              <TextBlock.Text>
                <Binding Path="Name"/>
              </TextBlock.Text>
              <TextBlock.FontStyle>
                <Binding Path="IsEmpty" Converter="{x:Static conv:BoolToItalic.Instance}"/>
              </TextBlock.FontStyle>
            </TextBlock>
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

  </DockPanel>
</UserControl>
